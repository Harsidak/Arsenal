#include <stdio.h>
#include <stdlib.h>

int bs(int arr, int length, int target){
    
}

int main(){
    int t;scanf("%d", &t);
    while(t--){
        int n;
        long long int h;
        scanf("%d %d",&n,&h);

        long long int arr[n];
        for(int i = 0;i < n;i++){scanf("%lld", &arr[i]);}

    }
    return 0;
}